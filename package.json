{
  "name": "sample",
  "version": "0.0.0",
  "description": "",
  "devDependencies": {
    "gh-pages": "^1.0.0"
  },
  "scripts": {
    "build": "bundle exec jekyll build",
    "deploy": "npm run build && gh-pages -d _site"
  }
}
